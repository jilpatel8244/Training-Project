<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>paginantion-component</title>
    <style>
        a {
            text-decoration: none;
            color: black;
        }

        a,span{
            padding: 10px;
        }

        .row {
            text-align: center; 
        }
    </style>
</head>

<body>
    <section>
        <div class="container">
            <div class="row">
                <% if(Number(current_page) != 1)  {%>
                    <a href="/app/v1/getQueryResult?page_no=1&orderBy_field=<%= orderBy_field %>&orderBy_type=<%= orderBy_type %>"> &#x3c;&#x3c; </a>
                    <a href="/app/v1/getQueryResult?page_no=<%= Number(current_page)-1 %>&orderBy_field=<%= orderBy_field %>&orderBy_type=<%= orderBy_type %>"> &#x3c;</a>
                <% }else{ %>
                    <a onclick="return false;"> &#x3c;&#x3c; </a>
                    <a onclick="return false;">&#x3c;</a>
                <% } %>

                <span><%= current_page %></span>

                <% if(Number(current_page) != Number(pageEnd))  {%>
                    <a href="/app/v1/getQueryResult?page_no=<%= Number(current_page)+1 %>&orderBy_field=<%= orderBy_field %>&orderBy_type=<%= orderBy_type %>"> &#x3e; </a>
                    <a href="/app/v1/getQueryResult?page_no=<%= Number(pageEnd) %>&orderBy_field=<%= orderBy_field %>&orderBy_type=<%= orderBy_type %>"> &#x3e;&#x3e; </a>
                <% }else{ %>
                    <a onclick="return false;"> &#x3e; </a>
                    <a onclick="return false;">&#x3e;&#x3e; </a>
                <% } %>
                
            </div>
        </div>
    </section>

</body>

</html>